# Merge upstream/main (v14.1.1) into patched (v13.17.6)

## Background

Our fork `patched` is based on v13.17.6 with ~108 custom commits. Upstream has reached v14.1.1 (major version bump through v14.0.0). 17 merge conflicts identified via `git merge-tree`.

### Our key custom features (most recent 4 commits)
1. `1f687d662` - fix(ai/agent): EventStream.fail() + agent-loop .catch() + Anthropic idle timeout
2. `d1494d5ec` - feat: bg_jobs status segment + /ps process dashboard + job-manager endTime/notifications
3. `71199a647` - fix: 53 type errors (keybindings dot-notation refactor)
4. `97e6d0846` - feat(bash): no timeout default + Ctrl+B to background

### Upstream breaking changes (v14.0.0)
- Chunk/edit API reworked (new `packages/coding-agent/src/edit/` modules)
- PendingActionStore replaced by ToolChoiceQueue
- searchDb removed from tool contexts
- Vim tool API changed
- Python exec replaced by JS vm tool
- Copilot OAuth switch

### Upstream notable additions
- Bash auto-background (`bash.autoBackground.enabled/thresholdMs`)
- `/rename` command + `session_name` status line segment
- `/force` command + ToolChoiceQueue
- Canonical model equivalence system
- SQLite read/write in tools
- OpenAI idle timeout bumped to 120s
- Multi-session ACP mode
- LSP startup discovery

## Strategy

**Merge** upstream/main into patched (not rebase -- 108 commits makes rebase impractical).

## Conflict Files (17 total)

### Trivial (take upstream, re-add our changes)
1. `CHANGELOG.md` - take upstream, prepend our entries
2. `packages/natives/native/index.js` - take upstream
3. `packages/coding-agent/src/stt/recorder.ts` - take upstream, reapply STT changes
4. `packages/coding-agent/src/stt/transcriber.ts` - same
5. `packages/coding-agent/src/tools/write.ts` - take upstream, reapply type fixes
6. `packages/coding-agent/src/extensibility/slash-commands.ts` - take upstream
7. `packages/coding-agent/src/main.ts` - take upstream

### ai/agent package (keep our fixes, upstream hasn't fixed these)
8. `packages/ai/src/utils/event-stream.ts` - keep ours (fail() + rejectFinalResult)
9. `packages/ai/src/utils/idle-iterator.ts` - merge both (upstream bumped OpenAI to 120s, we added Anthropic getter)
10. `packages/ai/src/providers/anthropic.ts` - take upstream base, reapply our iterateWithIdleTimeout wrapper
11. `packages/agent/src/agent-loop.ts` - keep our .catch() fix (upstream still has no catch)

### Heavy refactors (take upstream base, surgically reapply our additions)
12. `packages/coding-agent/src/config/keybindings.ts` - take upstream structure, add our 3 keys (bash.background, render.pause, render.resume)
13. `packages/coding-agent/src/tools/bash.ts` - take upstream (has auto-background), reapply no-timeout-default + Ctrl+B mechanism
14. `packages/coding-agent/src/sdk.ts` - take upstream, reapply our SDK additions (setBashBackgroundDeferred, backgroundBash, showPsDashboard)
15. `packages/coding-agent/src/modes/interactive-mode.ts` - take upstream, reapply bg_jobs wiring + Ctrl+B handler
16. `packages/coding-agent/src/modes/controllers/selector-controller.ts` - take upstream, re-add showPsDashboard
17. `packages/coding-agent/src/modes/controllers/input-controller.ts` - take upstream, re-add Ctrl+B handler

### Non-conflicting but need attention
- `packages/coding-agent/src/modes/components/status-line.ts` - CONFLICT: take upstream, re-add bgJobCount field
- `packages/coding-agent/src/modes/components/status-line/presets.ts` - CONFLICT: take upstream, add bg_jobs to presets
- `packages/coding-agent/src/modes/components/status-line/segments.ts` - add bg_jobs segment alongside upstream's session_name
- `packages/coding-agent/src/modes/components/status-line/types.ts` - add bgJobCount to SegmentContext
- `packages/coding-agent/src/config/settings-schema.ts` - add bg_jobs to StatusLineSegmentId union
- `packages/coding-agent/src/modes/theme/theme.ts` - take upstream, re-add icon.bgJobs + statusLineBgJobs color
- `packages/coding-agent/src/session/agent-session.ts` - take upstream, re-add asyncJobManager getter
- `packages/coding-agent/src/modes/types.ts` - add showPsDashboard to InteractiveModeContext
- `packages/coding-agent/src/slash-commands/builtin-registry.ts` - add /ps command
- `packages/coding-agent/src/modes/components/status-line-segment-editor.ts` - add bg_jobs entry
- `packages/coding-agent/test/status-line-path.test.ts` - add bgJobCount to mock
- 100 theme JSON files - add statusLineBgJobs key (should auto-merge)
- 5 new ps/ files - no conflict (new directory)

## Execution Phases

### Phase 1: Start merge on throwaway branch
```bash
git checkout -b merge-v14 patched
git merge upstream/main --no-commit
```
Assess actual conflict markers.

### Phase 2: Trivial conflicts (files 1-7)
For each: `git checkout upstream/main -- <file>` then manually re-add our small changes.
Estimated: <1h

### Phase 3: ai/agent fixes (files 8-11)
- event-stream.ts: keep ours entirely (upstream didn't change it structurally)
- idle-iterator.ts: take upstream, add our getAnthropicStreamIdleTimeoutMs() and DEFAULT_ANTHROPIC constant
- anthropic.ts: take upstream as base, find the SSE for-await loop, wrap with iterateWithIdleTimeout + firstItemTimeoutMs:0
- agent-loop.ts: keep ours (just adds .catch to IIFEs, upstream didn't change structure)
Estimated: 1-2h

### Phase 4: Keybindings (file 12)
Take upstream's structure. Add our 3 keybinding entries into their AppKeybindings interface.
Estimated: 30min

### Phase 5: Bash tool integration (files 13 + bash-executor + tool-timeouts)
Take upstream bash.ts (has auto-background). Reapply:
- No-timeout default in tool-timeouts.ts (0 = unlimited)
- Ctrl+B backgroundPromise mechanism in bash-executor.ts
- Wire Ctrl+B into foreground execution path

**Decision**: Keep both auto-background AND Ctrl+B. They're complementary:
- Auto-background: AI-initiated, timer-based, automatic
- Ctrl+B: user-initiated, manual
- /ps dashboard: visibility for both

When autoBackground is enabled with timeout=0, use thresholdMs as auto-background timer.
When Ctrl+B pressed during auto-background wait, immediately background.
Estimated: 3-5h

### Phase 6: Status line + bg_jobs segment (non-conflicting additions)
Take upstream's status-line.ts, segments.ts, presets.ts. Add:
- bg_jobs segment definition in segments.ts
- bg_jobs to presets
- bgJobCount to SegmentContext
- statusLineBgJobs color + icon.bgJobs to theme.ts
Estimated: 1-2h

### Phase 7: Heavy refactors (files 14-17)
For sdk.ts, interactive-mode.ts, selector-controller.ts, input-controller.ts:
Take upstream as base. Our changes are small/additive relative to upstream's structural rewrites.
Surgically reapply our SDK interface additions, bg_jobs wiring, Ctrl+B handlers, /ps dashboard integration.
Estimated: 3-5h

### Phase 8: agent-session + types + registry
- agent-session.ts: take upstream, re-add asyncJobManager getter + job count callback
- modes/types.ts: add showPsDashboard
- builtin-registry.ts: add /ps command
- status-line-segment-editor.ts: add bg_jobs entry
Estimated: 1h

### Phase 9: Theme and JSONs
- theme.ts: take upstream (simplified), add statusLineBgJobs + icon.bgJobs
- 100 JSON files: should auto-merge (additive key only)
Estimated: <1h

### Phase 10: Type check and fix
```bash
bun install --backend=symlink --force
bun check:ts
```
Fix any remaining type errors from merge mismatches.
Estimated: 1-3h

## Verification

1. `bun install --backend=symlink --force`
2. `bun check:ts` -- must pass clean
3. `bun test test/status-line-path.test.ts` -- status line test
4. Manual smoke tests:
   - Start session, verify status line renders with bg_jobs + session_name segments
   - Run long bash command, verify Ctrl+B backgrounds it
   - Enable autoBackground, verify long commands auto-background
   - Open /ps dashboard, verify it shows jobs from both paths
   - Verify /rename works alongside /ps
   - Kill network during Anthropic stream, verify fail() propagates

## Key Risks

1. **sdk.ts** (1660-line upstream diff) is the riskiest merge -- our additions are small but the base changed extensively
2. **selector-controller.ts** ps dashboard integration may need rework against upstream's new structure
3. **No-timeout default + auto-background** interaction needs careful testing
4. Some of our 53 type error fixes (commit 71199a647) may conflict with upstream's own refactors to the same code

## Total Effort Estimate
2-4 days for experienced developer, split across 10 phases.
